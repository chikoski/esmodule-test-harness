# esmodule-test-harness

A test harness for ECMAScript modules. 

* Test ECMAScript modules (a.k.a. ES6 modules) without transpiler usage
* [mocha.js](http://mochajs.org/) like DSL to define test cases

